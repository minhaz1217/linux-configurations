# Deploying a dotnet application using ansible

## Run the playbook using.
`sudo ansible-playbook -i hosts -e 'ansible_ssh_private_key_file="/mnt/c/Users/HA HA/amazon_key"'  deploy_dotnet_application.yml`


